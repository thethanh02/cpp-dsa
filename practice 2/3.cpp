#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    cin.ignore();
    while (t--) {
        int n;
        cin >> n;
        int a[n];
        for (int i = 0; i < n; i++) cin >> a[i];
        stack<int> st;
        st.push(a[0]);
        for (int i = 1; i < n; i++) {
            
        }
    }
}